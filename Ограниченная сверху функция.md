Функция называется ограниченной сверху если 
$$
\exists M : \forall x \in X f(x) \leq M
$$

Функция называется ограниченной снизу если
$$
\exists m: \forall x \in X f(x) \geq m
$$

Иными словами множество всех значений функции $y = f(x)$ ограничено

[[Ограниченные множества]]